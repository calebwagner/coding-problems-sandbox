function lineThoughPoints(points) {
    let lengthOfPoints = points.length;
    if (lengthOfPoints <= 2) return lengthOfPoints

    var res = 2;

    for (let i = 0; i < lengthOfPoints - 1; i++) {
        let m = {
            _: 0
        };
        dup = 1;

    }
}